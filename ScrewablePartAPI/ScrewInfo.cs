using MSCLoader;
using UnityEngine;

namespace ScrewablePartAPI
{
    public class ScrewInfo : MonoBehaviour
    {
        public int tightness { get; set; } = 0;
        public int size { get; set; } = 0;
        // Use this for initialization
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}